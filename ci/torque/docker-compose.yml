version: "2"

services:

  torque:
    image: agaveapi/torque
    build: .
    container_name: torque
    hostname: torque
    environment:
      - DPDISPATCHER_TEST=torque
    volumes:
      - ../..:/dpdispatcher
    tty: true
    privileged: true
